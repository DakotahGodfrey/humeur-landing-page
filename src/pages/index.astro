---
import LandingPage from "@/components/LandingPage.astro"

/* en-CA, unprefixed. `prefixDefaultLocale` is false, so the default locale
   serves from the root and every other locale from src/pages/[locale]/. */
---

<LandingPage />
